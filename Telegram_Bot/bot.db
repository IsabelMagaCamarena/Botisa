import sqlite3
 
# Connecting to sqlite
# connection object
connection_obj = sqlite3.connect('bot.db')
 
# cursor object
cursor_obj = connection_obj.cursor()
 
# Drop the bot table if already exists.
cursor_obj.execute("DROP TABLE IF EXISTS BOT")
 

cursor_obj.execute(table)
 
print("Table is Ready")
 
# Close the connection
connection_obj.close()